name: QA

on:
  pull_request:
    branches: [main]
  schedule:
    # UTC 02:00 daily (nightly full QA)
    - cron: '0 2 * * *'
  workflow_dispatch:

jobs:
  cli-e2e:
    name: CLI End-to-End Smoke Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: 'pip'
      - name: Install package
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e .
      - name: Run CLI E2E tests
        run: |
          python scripts/qa/run_cli_e2e.py --json qa-cli-e2e-results.json
      - name: Upload CLI E2E results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: qa-cli-e2e-results
          path: qa-cli-e2e-results.json

  coverage-audit:
    name: Coverage Audit (target 80%)
    # CI 容量工程（2026-08-19）：coverage-audit 是全量 pytest + 覆盖率统计的最重 job，
    # PR 时段与 ci.yml test 串行跑两份全量 pytest 导致自建 runner（memory-runnerz 组，
    # 仅 1 台 Linux runner）排队 ~7min。事件门：仅 schedule/workflow_dispatch 运行，
    # PR 时段跳过、夜间全量保留——pve 每 PR 负载立减一半（7min→3.5min）。
    # 扩容指针：第二台 Linux runner 属 PVE 侧 runbook 2026-08-18-pve-runner-sop，
    # 不进本 mission。qa-ok 聚合已处理 skipped 状态（不误报失败）。
    # 对齐 full-regression 的既有先例。
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    # pytest 全量 + 覆盖率统计是 CI 最重 job，切自建 runner（ce-01）
    runs-on: [self-hosted, pve-linux]
    env:
      PIP_BREAK_SYSTEM_PACKAGES: 1
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1  # 浅克隆，加速 checkout（自建 runner 走代理隧道，全量历史拉取慢）
      # 自建 runner：扩展 PATH 包含 ~/.local/bin（pip 安装 console scripts 的位置）
      - name: Extend PATH for self-hosted runner
        run: echo "/home/runner/.local/bin" >> "$GITHUB_PATH"
      # 自建 runner：系统已预装 Python 3.12 + pytest 全家桶，直接用系统解释器。
      # 不能保留 if 恒假的 setup-python 引用：runner 启动阶段预下载所有 action，慢网络下超时杀死 job。
      - name: Use system Python (self-hosted)
        run: |
          python3 --version
          echo "/usr/bin" >> "$GITHUB_PATH"
      - name: Install package and test deps
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e .
          python -m pip install pytest pytest-cov pytest-rerunfailures pytest-xdist
      - name: Run pytest with coverage (gated at current threshold)
        # 门禁 pytest 在前：生成权威 coverage.xml（也是 gap 分析的数据源）
        env:
          # Isolate coverage data files to prevent cross-job interference on shared runners
          COVERAGE_FILE: ${{ runner.temp }}/cov-${{ github.run_id }}-${{ github.run_attempt }}/.coverage
        run: |
          mkdir -p "$(dirname "$COVERAGE_FILE")"
          # CI 串行（-n 0）裁决依据（2026-08-20 编排器裁定）：
          # - A/B 对照实锤 runner-01 上 xdist 分片丢失：同 job 串行 TOTAL 14298/miss 2288=84%
          #   vs 并行 miss 8087=43%；runner-02 隔离后串行同样 84%
          # - 串行在两 runner 上均实测 84% 正确，夜间 job 时间不敏感
          # - 本地 -n 6 并行验证标准不变（addopts 未动）
          # - COVERAGE_FILE 隔离 env 保留，保护未来恢复并行
          # - 不违反 VAL-COV-015（该断言禁 no:randomly/固定 seed，不禁分发模式）
          python -m pytest tests/ -n 0 --cov=memory_core --cov-report=xml --cov-branch --cov-fail-under=80
      - name: Run coverage gap analysis
        # 复用门禁 pytest 的 coverage.xml（--reuse-xml），不再内部串行重跑全量 pytest。
        # 旧模式在自建 runner 上必挂：内部串行 pytest 超 timeout=600 产出半成品 XML
        # → "no modules found in coverage XML" → json 不产出（被 || true 吞掉）。
        # Note: target 80% is aspirational; CI reports gap but does not fail
        # until coverage is progressively raised from 15% baseline.
        run: |
          cp coverage.xml coverage_gap.xml
          python scripts/qa/coverage_gap_finder.py --target 80 --json qa-coverage-results.json --reuse-xml || true
      - name: Upload coverage XML
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: qa-coverage-results
          path: |
            coverage.xml
            qa-coverage-results.json

  hook-lifecycle:
    name: Hook Lifecycle Integration Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: 'pip'
      - name: Install package and test deps
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e .
          python -m pip install pytest pytest-cov
      - name: Hook gateway & lifecycle tests
        run: |
          python -m pytest tests/ -k "hook or gateway or lifecycle or session" -v --tb=short --no-header -q --no-cov
      - name: PreToolUse guard tests
        run: |
          python -m pytest tests/ -k "pretooluse or guard" -v --tb=short --no-header -q --no-cov
      - name: Telemetry tests
        run: |
          python -m pytest tests/ -k "telemetry or posthog or metrics" -v --tb=short --no-header -q --no-cov
      - name: Integrity tests
        run: |
          python -m pytest tests/ -k "integrity or resign or manifest or signing" -v --tb=short --no-header -q --no-cov

  business-policy:
    name: Business Policy & Guard Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: 'pip'
      - name: Install package and test deps
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e .
          python -m pip install pytest pytest-cov
      - name: Business policy tests
        run: |
          python -m pytest tests/ -k "business_policy or rule or marker" -v --tb=short --no-header -q --no-cov
      - name: Guard classification tests
        run: |
          python -m pytest tests/ -k "guard_classify or guard_pattern or denylist" -v --tb=short --no-header -q --no-cov
      - name: Scope resolver tests
        run: |
          python -m pytest tests/ -k "scope or resolver or routing" -v --tb=short --no-header -q --no-cov

  schema-migration:
    name: Schema & Migration Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: 'pip'
      - name: Install package and test deps
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e .
          python -m pip install pytest pytest-cov
      - name: Migration tests
        run: |
          python -m pytest tests/ -k "migrate or migration" -v --tb=short --no-header -q --no-cov
      - name: Schema tests
        run: |
          python -m pytest tests/ -k "schema or adapter or index_schema or template" -v --tb=short --no-header -q --no-cov
      - name: Version sync tests
        run: |
          python -m pytest tests/ -k "version_sync or upgrade or hook_upgrade" -v --tb=short --no-header -q --no-cov

  boundary-security:
    name: Boundary & Security Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: 'pip'
      - name: Install package and test deps
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e .
          python -m pip install pytest pytest-cov
      - name: Boundary tests
        run: |
          python -m pytest tests/ -k "boundary or readonly or pollution or source_repo" -v --tb=short --no-header -q --no-cov
      - name: Denylist & guard tests
        run: |
          python -m pytest tests/ -k "denylist or guard_patterns or evidence" -v --tb=short --no-header -q --no-cov
      - name: Boundary check script
        run: |
          python scripts/check_boundary.py
      - name: Repo health check
        run: |
          bash scripts/repo_health_check.sh --ci

  full-regression:
    name: Full Regression (Nightly only)
    runs-on: ubuntu-latest
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: 'pip'
      - name: Install package and all deps
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"
      - name: Full test suite with coverage
        # 加 xml 报告：供下方 gap 分析复用（--reuse-xml），避免内部再串行重跑全量
        env:
          # Isolate coverage data files to prevent cross-job interference on shared runners
          COVERAGE_FILE: ${{ runner.temp }}/cov-${{ github.run_id }}-${{ github.run_attempt }}-full/.coverage
        run: |
          mkdir -p "$(dirname "$COVERAGE_FILE")"
          python -m pytest tests/ --cov=memory_core --cov-report=term-missing --cov-report=xml --cov-branch --durations=20
      - name: Full integration test
        run: |
          python scripts/test_full_integration.py --verbose || code=$?
          if [ "${code:-0}" = "1" ]; then exit 1; fi
      - name: CLI E2E all layers
        run: |
          python scripts/qa/run_cli_e2e.py --json qa-full-e2e.json
      - name: Coverage gap analysis
        run: |
          cp coverage.xml coverage_gap.xml
          python scripts/qa/coverage_gap_finder.py --target 80 --json qa-full-coverage.json --reuse-xml || true
      - name: Dependency security scan
        run: |
          pip-audit --progress-spinner off
      - name: TODO/FIXME scan
        continue-on-error: true
        run: |
          grep -rn --include="*.py" -E "TODO|FIXME" memory_core/ scripts/ || true
      - name: Upload full QA artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: qa-full-regression-results
          path: |
            qa-full-e2e.json
            qa-full-coverage.json

  qa-ok:
    name: qa-ok
    runs-on: ubuntu-latest
    needs: [cli-e2e, coverage-audit, hook-lifecycle, business-policy, schema-migration, boundary-security]
    if: always()
    steps:
      - name: Verify QA jobs
        run: |
          echo "cli-e2e: ${{ needs.cli-e2e.result }}"
          echo "coverage-audit: ${{ needs.coverage-audit.result }}"
          echo "hook-lifecycle: ${{ needs.hook-lifecycle.result }}"
          echo "business-policy: ${{ needs.business-policy.result }}"
          echo "schema-migration: ${{ needs.schema-migration.result }}"
          echo "boundary-security: ${{ needs.boundary-security.result }}"

          if [ "${{ needs.cli-e2e.result }}" != "success" ]; then
            echo "FAIL: cli-e2e did not succeed"; exit 1
          fi
          # coverage-audit 被 schedule 事件门跳过时结果为 'skipped'，接受 success 或 skipped
          if [ "${{ needs.coverage-audit.result }}" != "success" ] && [ "${{ needs.coverage-audit.result }}" != "skipped" ]; then
            echo "FAIL: coverage-audit did not succeed or skip"; exit 1
          fi
          if [ "${{ needs.hook-lifecycle.result }}" != "success" ]; then
            echo "FAIL: hook-lifecycle did not succeed"; exit 1
          fi
          if [ "${{ needs.business-policy.result }}" != "success" ]; then
            echo "FAIL: business-policy did not succeed"; exit 1
          fi
          if [ "${{ needs.schema-migration.result }}" != "success" ]; then
            echo "FAIL: schema-migration did not succeed"; exit 1
          fi
          if [ "${{ needs.boundary-security.result }}" != "success" ]; then
            echo "FAIL: boundary-security did not succeed"; exit 1
          fi
          echo "All QA jobs succeeded."
