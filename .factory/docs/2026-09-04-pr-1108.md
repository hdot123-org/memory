## 已完成的代码修复

### 修复 1：AST 源码级漂移守护
**文件**：`tests/test_full_integration_hook_entries.py:326-373`

**改动**：
- 重写 `test_fallback_supported_hosts_matches_constants` 测试
- 从运行时属性断言改为 `ast.parse` 源码级守护
- 解析 `scripts/test_full_integration.py` 的 `except ImportError` 处理器
- 提取 `SUPPORTED_HOSTS` 字面量赋值（Tuple[Constant...]）
- 断言字面量元组与 `memory_core.constants.SUPPORTED_HOSTS` 相等

**效果**：防止字面量静默漂移到历史值（如 `('factory','codex','claude')`），运行时属性断言的同义反复缺陷已消除。

### 修复 2：factory/codex 分支类型守卫
**文件**：`scripts/test_full_integration.py:231-235`

**改动**：
```python
# 修改前
for event in expected_events:
    if event in hooks and hooks[event]:
        found_events.add(event)

# 修改后
hooks_dict = hooks if isinstance(hooks, dict) else {}
for event in expected_events:
    if event in hooks_dict and hooks_dict[event]:
        found_events.add(event)
```

**效果**：非可迭代 `hooks` 值（如 `42` / `null`）返回 FAIL CheckResult 而非 TypeError 崩溃，镜像 zcode 分支 `:238` 的 isinstance 守卫写法。

### 新增测试用例
**文件**：`tests/test_full_integration_hook_entries.py:375-407`

新增 `test_factory_non_iterable_hooks_no_type_error`：
- 测试 `hooks=42` 和 `hooks=None` 场景
- `wrapper_installed=True` → 期望 FAIL（不崩溃）
- `wrapper_installed=False` → 期望 WARN（CI runner 降级）

## 验证状态

**未能完成本地验证**：当前会话无命令执行工具可用，无法运行：
- `pytest tests/test_full_integration_hook_entries.py`
- `ruff check scripts/test_full_integration.py tests/test_full_integration_hook_entries.py`
- `mypy --strict scripts/`

**CI 将验证**：推送到 PR #1108 后，CI 会运行完整测试套件。

## 待提交与推送

需要执行（当前会话无法执行）：
```bash
git add scripts/test_full_integration.py tests/test_full_integration_hook_entries.py
git commit -m "fix: PR #1108 漂移守护重写为 AST 源码级 + factory 分支类型守卫

- 重写 test_fallback_supported_hosts_matches_constants 为 ast.parse 源码级守护，
  消除运行时属性断言的同义反复缺陷
- factory/codex 分支增加 isinstance(hooks, dict) 守卫，防止非可迭代值 TypeError
- 新增 test_factory_non_iterable_hooks_no_type_error 钉住用例

Fixes drift-guard-source-level-fix"
git push origin feat/ci-script-hardening
bash scripts/write-pending-ci.sh --source session --context 'PR1108 漂移守护修复' 1108
```